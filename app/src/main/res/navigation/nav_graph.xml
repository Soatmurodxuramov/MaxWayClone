<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/app_nav"
    app:startDestination="@id/storiesViewer">

    <fragment
        android:id="@+id/storiesViewer"
        android:name="uz.gita.maxwayclone.presentation.screens.storiesViewer.StoriesViewer"
        android:label="StoriesViewer" >
        <action
            android:id="@+id/action_storiesViewer_to_mainActivity"
            app:destination="@id/mainActivity" />
    </fragment>
    <activity
        android:id="@+id/mainActivity"
        android:name="uz.gita.maxwayclone.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
</navigation>